# Normalise line endings. Development happens on Windows, CI and every
# deployment target run on Linux, and a CRLF that reaches a shell script or a
# container entrypoint fails in a way that reads as a mystery.
* text=auto eol=lf

# Binary-ish files git should not touch.
*.png binary
*.jpg binary
*.pem binary
